par_cmd.py is a program that will execute a given command when it notices action on pin13 on the parallell port. It will recognize if it's several actions on a short time of periode or if it's one long one.
